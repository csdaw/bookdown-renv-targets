# Using renv {#renv}

```{r}
#### renv_load() - Load the renv library ####
chapter <- "07-renv"

proj_root <- rprojroot::find_root("_bookdown.yml")

renv_path <- paste0(proj_root, "/_renv/", chapter)

print(renv_path)
#renv::load(project = renv_path)
```

This is a test chapter to see if renv works.

```{r}
#library(dplyr)
#ToothGrowth %>% select(len)
```
